# Optional local overrides. .env.local is gitignored (*.local).
# Production: GitHub secret VITE_LEADERBOARD_WRITE_KEY (deploy workflows).
# API URL defaults to https://leaderboards-opal.vercel.app

VITE_LEADERBOARD_API=https://leaderboards-opal.vercel.app
VITE_LEADERBOARD_WRITE_KEY=copy-gigazonk-value-from-leaderboards-WRITE_KEYS
